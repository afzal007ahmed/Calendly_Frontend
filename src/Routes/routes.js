export const routes = {
  scheduling: "/",
  availability: "/availability",
  meetings: "/meetings",
  login: "/login",
  register: "/register",
  redirect: "/redirect",
  profile:"/profile",
  booking : "/book/:username/:userId/:scheduleId"
};

